# 项目目标与总体架构

> 文档状态：现行唯一产品总纲。本文所说的“总体架构”是产品目标和能力骨架，不是代码架构。
>
> 本文只记录已经确认的产品目标、能力和表达边界，不涉及代码、接口、数据库或部署架构。港口范围比例以[港口比例功能说明](./港口比例功能说明.md)为准；燃料路径、固定因子、资格和默认估算条件以[燃料因子库规范](./燃料因子库规范.md)为准；单位、计算公式、状态、输出和测试契约以[MVP计算规格](./docs/superpowers/specs/2026-08-07-voyage-fuel-decision-calculation-spec.md)为准。这些文档分别固定不同层次，发生冲突时按对应权威层级处理。旧代码和算法原型只供参考，不限制新项目设计。
>
> [新能源混兑经济性决策层设计](./docs/superpowers/specs/2026-09-16-new-energy-decision-layer-design.md)承接金老师补充的第一阶段目标，规定新能源用量、相对 B0 的经济拆分、三类条件式推荐和统一结果交付。该决策层复用既有计算结果，不另设能源、排放、EU ETS 或 FuelEU 公式；底层计算口径仍以 MVP 计算规格为准。
>
> 本次产品总纲更新：2026-09-17，按当前开发分支已实现能力补充；不代表代码已合并主分支或真实业务验收已经完成。

## 1. 产品目标与边界

产品面向船东、租家、燃油采购和燃料供应相关人员，基于航次范围、燃料条件和价格，比较不同燃料类型及混兑方案对成本、EU ETS 和 FuelEU 表现的影响。

首期以报告年份和两个相邻有效 `Port of Call` 之间的一个航段为计算范围。用户可以用该航段测算支持当前航次或本次加油决策。在缺少完整年度数据时，产品提供航次级测算、多方案比较、关键比例和条件式建议，不输出 FuelEU 年度应付罚款、确定的全年最优方案或正式年度合规结论。

候选燃料可为空，也可包含一个或多个独立候选。没有候选时，仅计算和导出未混兑基准 B0 的能源、排放、EU ETS 和 FuelEU 结果，不生成新能源决策摘要或条件式推荐；存在候选时，在同一基准下开展方案比较。

## 2. 正式总体骨架

项目采用“两个固定基础、四项核心能力、一项贯穿保障、三个未来升级阶段”的总体骨架。

```text
港口与范围规则 ─┐
                ├→ 航次决策案例
燃料因子与证据 ─┘        ↓
                    单方案评估
                         ↓
                    多方案比较
                         ↓
                   2.5级条件式决策
                         ↓
                 可解释、可复核的结果

未来接入：
真实年度状态 → 年度滚动规划 → 船队与合规机制
```

### 2.1 两个固定基础

1. **港口与范围规则**：根据年份和航次判断 EU ETS、FuelEU 各自的适用范围和比例；
2. **燃料因子与证据**：确定具体燃料路径使用的因子、认证要求以及结果的证据状态。

### 2.2 四项核心能力

1. **航次决策案例**：首期由报告年份、两个相邻有效 `Port of Call` 及其对应航段构成，承载该航段的一次燃料选择问题，为各候选方案提供共同的比较背景；
2. **单方案评估**：回答一个具体燃料方案会产生什么成本、EU ETS 和 FuelEU 结果；
3. **多方案比较**：按相同口径比较候选方案、相对基准变化、有效方案和关键比例；
4. **2.5级条件式决策**：输出不同目标和条件下的方案排序、参考点和切换临界点。

2.5级是多方案比较之上的决策能力，继续复用同一方案评估路径。

### 2.3 一项贯穿保障

计算依据、数据状态和结果边界贯穿全部能力，包括港口判断依据、燃料因子和认证、用户输入与估算值、成本包含范围、方案可执行性、航次与年度边界以及结论敏感性。

页面、CSV 和 PDF 使用同一次成功计算的统一原始结果。输入变更后，页面使旧结果失效；显示精度仅影响展示，不改变计算值、排序、状态或推荐。关键空值保留原因码，区分缺少输入、不适用、目标不可达和计算阻断等情况。

### 2.4 三个未来升级阶段

1. **3级：真实年度状态建议**：接入年初至今的能源、GHGI或合规余额，重新判断当前航次；
2. **4级：年度滚动规划**：加入未来航次、采购机会、价格区间和剩余能源需求；
3. **5级：年度及船队合规**：处理 Banking、Borrowing、Pooling、年度罚款和船队分配。

产品的中心对象是一次航次决策案例。EU ETS 和 FuelEU 是同一燃料方案的两组评价结果；港口规则与燃料因子是共同基础，不作为彼此孤立的产品。

## 3. 默认决策输出

产品不询问费用和合规收益最终归属，也不进行用户画像或个性化推荐。系统统一拆分并展示各项结果，由用户结合自身合同和业务情况判断。

默认回答以下问题：

1. 当前模型纳入成本最低的方案；
2. 达到用户指定要求时成本最低的方案；
3. 在给定预算、供应量和允许混兑范围内改善最多的方案；
4. 燃料价格、EUA 价格或 FuelEU 合规改善价值处于不同区间时，方案如何切换。

同时展示燃料成本变化、EUA 数量及成本变化、FuelEU 合规表现改善、方案差异和切换临界点。单位合规改善成本、本航次 GHGI 参考点和最大改善点属于比较指标或参考边界，不单独作为默认推荐。

新能源决策层具体回答“加多少、多花多少、节省多少、净成本如何变化”，同时保留绝对结果和相对 B0 的变化：

- 候选燃料用量（吨）及候选燃料质量占比，即候选燃料质量除以混合燃料总质量；
- 新增燃料成本：候选方案燃料采购成本减去 B0 燃料采购成本；
- EU ETS 成本节省：B0 EUA 成本减去候选方案 EUA 成本；
- 净成本变化：候选方案当前模型成本减去 B0 当前模型成本，负值表示更便宜，正值表示额外支出；
- FuelEU GHGI、合规余额和航次指示性金额的变化，单独呈现，不并入现金节省或当前模型成本。

在相应价格、目标适用性和约束条件满足时，分别给出成本优先、达标优先和最大合规改善三类结果，并展示选中方案的用量、质量占比及条件式建议依据。“最大合规改善”只指本模型的 FuelEU 合规余额改善，不代表独立物理生命周期减排。候选燃料临界价格、EUA 临界价格及其状态用于解释经济边界；合规改善参考价值切换点用于比较固定报告方案集合中的选择变化。

## 4. 燃料方案与混兑反算

吸收市场上燃料计算器和“Biofuel Simulator”类工具中能够在当前产品尺度内成立的能力：

1. 用户输入报告年份、两个相邻有效 `Port of Call`、基准燃料及用量，可选填候选燃料、价格和允许混兑范围；缺少价格时保留能源、排放及合规计算，不生成相应经济结论；
2. 系统始终保留未混兑基准方案，并按相同航次能源需求比较不同方案；
3. 对一种基准燃料和一种候选燃料，在已经确认允许混兑的范围内连续求解目标比例、预算/供应量/最大混兑比例等边界；
4. 只展示固定报告方案集合中的关键比例、用户指定比例和约束边界，不展示所有连续比例；
5. 可以比较多个独立的燃料路径或报价，但单个首期混兑方案只包含一种基准燃料和一种候选燃料；
6. 分别计算达到本航次当年 GHGI 参考线所需的最低混兑比例，以及达标方案中当前模型成本最低的比例；
7. 比较候选燃料增量成本与 EU ETS 对应的 EUA 成本变化；
8. 输出 FuelEU 航次级按比例分配的 GHGI 和合规余额估算，以及相对基准方案的改善量；
9. 计算候选燃料临界价格、成本与合规改善的有效方案，以及不同合规改善价值下的方案切换点；
10. 按具体燃料路径和证据计算 FuelEU 法规口径的 WtT/TtW/WtW（GHGI）结果，并明确区分 FuelEU 合规改善与独立物理生命周期减排；独立物理 WtW 口径留待后续生命周期模块。

完整因子库有37条路径，其中`ELECTRICITY_OPS`不进入本期航段输入；MVP开放36条航行燃料路径。混兑功能只对用户指定并确认允许混兑的组合开放；燃料因子库只提供路径和计算因子，不证明任意两种燃料可以安全混兑。兼容性、供应或认证尚未确认时，方案仍可计算，但不能标记为“可执行方案”。

## 5. 单方案评估

单方案评估回答“在当前年份和两个相邻有效 `Port of Call` 之间，采用这一种具体燃料方案会产生什么结果”。每个候选方案都通过同一过程计算。

默认主要结果包括：

- 燃料用量、能源和燃料采购成本；
- EU ETS 范围内排放、EUAs和EUA成本；
- FuelEU航次级按比例分配估算的GHGI、当年目标和合规余额；
- 使用的港口规则、燃料路径、因子、认证和结果状态；
- 尚待确认的兼容性、供应、认证和其他执行条件。

执行状态统一显示为 `EXECUTION_CONDITIONS_PENDING`（执行条件待确认）。该状态不阻止数学测算，但结果不得表述为采购建议或已经可执行的方案。

当前模型纳入成本为燃料采购成本与EUA成本之和。FuelEU年度罚款不进入该成本。

系统保留并展示“FuelEU航次比例情景下的指示性罚款等值”，作为用户比较生物燃料溢价与潜在FuelEU压力的辅助结果。它可以出现在方案比较结果中，也可以显示相对基准方案的“参考等值减少”，但不进入当前模型成本或默认方案排序。必须明确说明：

- 该数值是把当前航段比例估算单独代入罚款公式得到的参考值；
- 该数值不等于本航次真实应付罚款；
- 该数值可以回答“相对基准方案，理论上减少多少FuelEU罚款等值”，但不能表述为已实现的现金节省；
- 该数值不能与其他航次的指示性罚款等值相加；
- 该数值不包含年度能源分配、Banking、Borrowing和Pooling；
- 获得完整年度数据后，必须合并年度状态重新计算。

单方案评估只回答一个方案会产生什么结果，不负责比较或推荐。

## 6. 2.5级条件式建议

2.5级采用统一的“标准条件表 + 切换临界点”形式，不做用户画像或个性化推荐，也不在年度信息不足时强行选出一个综合最优方案。

标准条件包括：

- 只看当前模型纳入成本时，哪个方案最省；
- 要达到当年 GHGI 参考线时，最低需要多少混兑、哪个方案成本最低；
- 给定预算、供应量或最大混兑比例时，最多可以改善多少；
- 燃料价格、EUA价格或 FuelEU 合规改善参考价值达到什么程度时，方案会从 B0 切换到其他比例。

建议以类似以下方式表达：

```text
当前模型成本优先：B0
需要达到GHGI参考线：候选 A，候选燃料质量占比 24%
FuelEU合规改善参考价值超过X：候选 A，候选燃料质量占比 20%
FuelEU合规改善参考价值超过Y：候选 A，候选燃料质量占比 30%
```

年度状态可以作为解释条件，但不能直接固化成“有盈余选B0、存在缺口选B30”。FuelEU 指示性罚款等值可以作为参考比较结果展示，但不进入当前模型成本或默认方案排序。

## 7. 候选方案与可行性边界

候选方案是用户希望比较的一种具体燃料使用方式。纯燃料方案只包含一种燃料；首期每个混兑方案包含一种基准燃料和一种候选燃料，并明确混兑比例、燃料路径、价格和适用条件。

### 7.1 计算、比较和执行状态

燃料“能不能计算”和方案“能不能执行”分开判断。因子库中的目标燃料路径原则上都应当能够计算；认证、供应和兼容性主要决定结果状态和执行资格。

1. **可计算**：存在对应燃料路径和因子，必需数值已提供；使用核验证据时属于核验结果，使用明确场景假设时属于估算结果；
2. **可比较**：在可计算基础上，价格、范围和比较口径完整，可以进入经济排序；缺少价格时仍可做排放和合规比较，但不能做成本排序；
3. **执行条件待确认**：当前阶段统一为 `EXECUTION_CONDITIONS_PENDING`，系统不完成采购、船舶兼容性、真实供应或交付条件核验，不输出“可执行方案”。未来接入相应证据和核验能力后，才能另行定义可执行资格。

36条MVP航行路径都有可用的法规因子或默认估算参数，普通用户没有认证数据不会阻断测算。RFNBO路径仍须遵守资格分支：资格未证明时回退同类化石路径，用户明确假设合格时生成`ESTIMATED`情景。只有燃料路径不存在、关键数值无效，或使用内置库以外的路径却没有完整场景参数时，才进入“计算阻断”。可计算但尚未可执行的方案仍可展示，但必须标明其状态，不能直接表述为采购建议。

### 7.2 默认估算模式

用户选择燃料并填写用量和价格后即可计算。普通用户不需要填写PoS、PoC、E、eu或Cslip。

- 11条A级航行燃料直接使用法规因子；`ELECTRICITY_OPS`保留在完整库但不进入本期航段计算；
- 24条B级非RFNBO燃料默认使用参考网站参数；RFNBO路径按资格规则生成估算或化石回退；
- `UCO_FAME`使用项目已经确认的RED默认路径；
- 有实际批次或认证数据时，改用实际数据重新计算。

使用默认参数时，页面显示“按参考网站默认参数估算”；使用有效认证数据时，显示“按认证数据计算”。参考网站参数不能称为欧盟官方默认值。

Cslip 不作为普通用户输入。内置 LNG、液化生物甲烷（生物 LNG）和 e-LNG 按燃料库中的对应设备路径读取 Cslip；其他内置路径不套用甲烷滑移公式。自定义或覆盖数据对其他路径提供非零 Cslip 且没有明确支持的其他滑移气体规则时，只阻断该路径。LPG 和氨的参考网站 `Cslip=0` 仅用于 `ESTIMATED` 估算，正式核验必须提供适用的认可 Cslip。

这样，36条MVP航行燃料路径都能直接进入测算或资格回退流程。认证、供应、兼容性和交付条件仍然决定方案能否实际执行。详细参数见[燃料因子库规范](./燃料因子库规范.md#四默认估算规则)第四节。

### 7.3 计算阻断条件

出现以下情况时，方案不进入正常计算：

- 报告年份、两个有效 `Port of Call` 或范围判断无效；
- 没有可用的燃料路径和因子，且用户没有明确的场景假设；
- 没有航段燃料需求或能源需求；
- 混兑口径不明确，或体积混兑缺少实际密度；
- 混兑比例不在有效数值范围内。

价格缺失时可以继续做排放和合规比较，但不能生成当前成本排序、临界价格或经济性建议。

实际兼容性和交付能力由用户另行确认。系统按用户提供的最大比例、供应量和预算计算数学约束；超出约束的结果不能作为满足约束的建议。缺少价格导致预算无法验证时，明确标记预算未验证，不给出预算可行结论。数学约束满足也不意味着方案已可执行。

### 7.4 候选方案来源

候选方案可以来自：

- 用户指定的基准方案和混兑比例；
- 用户提供的燃料报价或供应商路径；
- 在允许范围内自动搜索得到的关键比例；
- 达到 GHGI 参考线、预算上限或最大混兑比例等边界点。

系统不把因子库中的所有燃料自动互相组合。因子库提供计算依据，不自动提供混兑兼容性或供应结论。

### 7.5 进入比较的原则

所有可计算方案都可以展示结果；满足价格、比较口径和相应约束要求的方案可以进入相应排序。当前阶段只提供中立比较、假设说明和条件式建议，同时展示计算、约束和执行状态；不输出实际采购行动建议或“可执行方案”结论。

## 8. 结果输出设计

结果采用“先说明边界，再展示差异，最后给出条件和依据”的层次。

### 8.1 结果边界

结果顶部始终说明：

- 当前计算的是哪个报告年份；
- 哪两个相邻有效 `Port of Call` 组成计算航段；
- EU ETS 和 FuelEU 各自采用的范围比例；
- 当前属于航次级测算、2.5级条件式建议还是年度级结果；
- 哪些关键数据是用户输入、系统默认、估算或待确认。

### 8.2 关键结论

不提供一个没有目标依据的“综合最优”，而是并列展示有明确含义的结论：

- 当前模型纳入成本最低的方案；
- 达到 GHGI 参考线的最低成本方案和所需比例；
- 给定预算、供应量或最大混兑比例时的最大改善方案；
- FuelEU 合规改善参考价值变化时的方案切换点。

### 8.3 方案比较

所有候选方案放在同一比较表中，至少包括：

- 燃料路径、候选燃料用量及候选燃料质量占比；
- 方案状态：可计算、可比较或计算阻断，另列约束状态和统一的执行条件待确认状态；
- 燃料用量、能源和燃料成本；
- 相对 B0 的新增燃料成本、EU ETS 成本节省和净成本变化；
- EU ETS 排放、EUAs和EUA成本；
- FuelEU 航次级 GHGI、合规余额和相对基准改善；
- FuelEU 指示性罚款等值及相对基准的参考变化；
- FuelEU 法规口径 WtW/GHGI；独立物理生命周期 WtW 排放或减排留待后续生命周期模块；
- 关键警告和待确认条件。

当前比较表统一使用 B0 作为经济比较基准，同时保留各方案的绝对结果，避免用户只能看到差额而无法复核。连续混兑只用于求目标比例、约束边界和最大改善边界；价格及合规改善价值切换只在固定报告方案集合中计算。该集合包含 B0、允许候选单独使用时的 B100、用户指定比例、目标比例和有效约束边界。达标方案中成本最低的比例作为结果字段保留，在线性成本模型下复用目标比例或有效约束边界，不另行展示连续搜索点。

### 8.4 条件式建议

单独展示“条件 → 方案 → 原因 → 假设”的表格，例如：

```text
当前模型成本优先       → B0  → 当前成本最低
需要达到GHGI参考线      → 候选 A，质量占比 24% → 达到参考线所需的最低成本比例
合规改善参考价值超过 X  → 候选 A，质量占比 20% → 增量成本开始具有参考价值
合规改善参考价值超过 Y  → 候选 A，质量占比 30% → 更高改善抵消额外成本
```

### 8.5 敏感性和依据

结果说明燃料价格、EUA价格、油耗、范围比例或因子证据变化到什么程度会改变方案排序，并指出当前最值得补充或确认的数据。

详细区域保留港口规则、燃料因子、认证证据、计算假设、指示性罚款公式和年度限制，支持复核但不干扰主结果。

### 8.6 同源结果与导出

- 一次成功计算形成统一原始结果，页面展示和 CSV、PDF 导出均使用该结果；导出读取服务端保存的结果快照，不重新计算，也不接受客户端伪造的计算结果。
- 修改航次、燃料、候选或约束等计算输入后，页面使旧结果失效，须重新计算后才能导出；快照过期或不可用时也须重新计算。
- 显示精度调整只改变页面和 PDF 的展示格式；CSV 保留原始数值，不因显示精度改变而改变。
- 关键空值的原因码随统一结果提供，并在页面、CSV 和 PDF 中呈现，避免把未知、不适用或无法求解误读为零。
- 没有候选燃料时，仍可导出 B0 结果，不附加新能源推荐。

## 9. 表达和法规边界

以下说法或能力不纳入当前航次级产品：

- 不把达到本航次 GHGI 参考线称为“零罚款方案”；
- 不把 FuelEU 指示性罚款等值作为本次真实现金支出；
- 不把 EUA 节省与 FuelEU 合规价值合并成一个笼统的“碳税节省”；
- 不在缺少完整年度数据时计算真实 FuelEU 应付罚款或避免罚款金额；
- 不在缺少年度状态和机制数据时直接推荐 Banking、Borrowing 或 Pooling；
- 不因燃料存在于因子库中就自动认定其可混兑、可供应或可获得正式合规收益；
- 不在缺少有效路径和认证证据时，把默认 RED 因子结果表述为已核证收益；
- 不自动组合任意多种燃料形成复杂混兑方案。

## 10. 当前一句话描述

> 输入报告年份、两个相邻有效 `Port of Call` 和基准燃料用量，即可进行 B0 航次级测算；加入一个或多个候选燃料及相应价格、比例和约束后，系统在相同能源需求下比较新能源用量、新增燃料成本、EU ETS 成本节省、净成本和 FuelEU 变化，并按适用条件给出成本优先、达标优先、最大合规改善结果及经济临界点。页面、CSV 和 PDF 使用同一计算结果，所有建议均保留执行条件待确认边界。
