# 示例就地教程验证报告

> 日期：2026-09-20
>
> 状态：功能与自动验证完成；真人新手验收待执行。
>
> 设计：[示例就地教程设计规格](../superpowers/specs/2026-09-20-contextual-example-tutorial-design.md)
>
> 计划：[示例就地教程实施计划](../superpowers/plans/2026-09-20-contextual-example-tutorial.md)

## 1. 交付内容

工作台中的两个已有合成示例现在会显示就地教程：

- 基础计算：说明成本组成、GHGI参考线、FuelEU余额和指示性金额。
- 多燃料比较：说明三类推荐、当前查看方案，以及限量UCO的供应限制。

指引以局部框选加短说明呈现，同一时间最多框选一个区域。“显示指引”可随时关闭，不会改变输入、结果、快照或导出状态。修改示例输入后，案例专属说明立即撤下。

配套说明页增加了“开始自己的计算”四步清单，并删除修改HVO供应量的练习要求。两个页面入口的案例说明链接改为新标签打开，避免丢失当前工作状态。

## 2. 实现边界

- 浏览器只使用现有API结果和展示模型，不读取 `expected.json`，不执行优化或重新计算。
- 未修改计算公式、推荐规则、业务请求合同及CSV/PDF数据合同。
- legacy视图保留原有计算、导出和说明页能力，不复制工作台高亮系统。
- 教程模块加载失败时，计算、结果和导出继续正常工作。
- 本次未创建教程中心、学习进度、弹窗链、截图教材或演示脚本。
- 本次未执行真实业务验收，也未安排至少3名新用户的无协助试用。

## 3. 自动验证结果

以下结果均来自最终代码状态下的实际运行。

| 检查 | 结果 |
|---|---|
| Python全量测试 | `287/287` 通过 |
| Node前端全量测试 | `51/51` 通过 |
| 独立业务案例全量核验 | `61/61` 通过 |
| 独立参考断言 | `412,517` 个 |
| 独立参考差异 | `0` |
| 参考进程生产模块导入 | `[]` |
| 业务案例CSV导出 | `60` 个 |
| 业务案例PDF导出 | `60` 个 |
| 示例浏览器独立对照 | A-2025、B-default完整API结果通过 |
| 示例CSV/PDF浏览器导出 | 两例均通过独立参考核验 |
| 故障注入 | 篡改赢家、比例、金额、供应状态均被拒绝 |
| 动态响应 | 注入错误赢家后，页面文字随响应变化且独立断言失败 |
| 教程模块故障隔离 | 计算、结果与导出保持可用 |
| 移动端 | `390px`、`320px` 无横向溢出 |
| 打包 | wheel包含两个教程模块、示例资源及说明页模板 |
| 安装包运行 | 从项目外使用已安装wheel请求教程资源和工作台，均返回 `200` |
| 差异检查 | `git diff --check` 通过 |

独立业务结果保存在 `output/contextual-example-tutorial/business-results.json`。

## 4. 画面检查

本次生成并人工检查了以下画面：

- `output/contextual-example-tutorial/basic-guide-desktop.png`
- `output/contextual-example-tutorial/basic-guide-detail.png`
- `output/contextual-example-tutorial/comparison-guide-mobile.png`
- `output/contextual-example-tutorial/comparison-guide-supply-mobile.png`

桌面画面中，成本说明、FuelEU余额说明和GHGI说明与对应结果相邻，框选没有改变表格尺寸。移动端供应说明保持可见，长文本正常换行，没有遮挡或页面横向溢出。

检查中发现并修复了一个问题：限量UCO位于默认折叠的“用量限制与价格影响”区域内，说明最初被挂到折叠内容内部，用户默认看不到。现在该说明会挂到折叠区外，并框选可见标题。

## 5. 尚未完成

- 尚未招募至少3名未参与开发的新用户进行无口头协助试用。
- 尚未测量真实用户完成首个示例的用时和误解点。
- 尚未验证真人用户能否在不知道答案的情况下正确解释三类推荐。
- 尚未执行真实报价、供应、认证或年度FuelEU结算业务验收。

因此，本报告可以认定自动功能、独立数值和内部交互验证完成，不能认定“新手已能上手”或“真实业务已验收”。

## 6. 结论

当前版本已经具备可加载、可核验、可在真实操作画面中阅读的教程流程。下一项非开发工作是按设计规格第8.2节进行小规模新手试用；在完成前，新手可用性状态保持“待验证”。
